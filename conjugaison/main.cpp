#include <iostream>

using namespace std;

int main()
{

    char verbe[20];
    int tailleVerbe;

    cout << "entrer votre verbe" << endl;

    cin >> verbe;

    tailleVerbe = strlen(verbe);

    if(tailleVerbe <= 2 || verbe[i-1] != 'r' || verbe[i-2] != 'e') {
        cout << "Le verbe n'est pas du premier groupe" << endl;
    } else {

    }


    return 0;
}
